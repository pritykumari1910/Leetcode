class Solution {
public:
    bool isPowerOfTwo(int n) {
       
    /*  if (n < 1) 
        return false;
        for (int i = 0; i <= n; i++)
        {
            if (pow(2, i) == n) 
               return true;
            if (pow(2, i) > n)
                return false;
        }
        return false;
    }
    
};*/

/*
for(int i=0;i<=30;i++)
{
    int ans = pow(2,i);
     if(ans == n){
         return true;
     }
}
return false;
    }

};
*/


return  (n>0 && (n & (n-1))==0) ? true:false;  
    }

};

       
       
        
        
    
