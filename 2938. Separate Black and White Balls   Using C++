class Solution {
public:
    long long minimumSteps(string s) {

        int n =s.size();
        long long ans=0,c=0;
        for(int i=0;i<n;i++){
            if(s[i]=='1')
                c++;
            else
               ans+=c;    
        }
        return ans;

        
    }
};
