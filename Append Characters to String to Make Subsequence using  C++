class Solution {
public:
    int appendCharacters(string s, string t) {
        int n1=t.size(),n=s.size(),i1=0;
        for(int i=0;i<n;i++)
        if(i1<n1 && s[i]==t[i1]) 
               i1++;
           return n1-i1; 
    }
};
